# ChangeNow API (서버 전용 — /api/swap 라우트에서만 사용)
CHANGENOW_API_KEY=your_key_here

# TronNRG API (서버 전용 — /api/energy 라우트에서만 사용)
TRONNRG_API_KEY=your_key_here

# ExchangeRate API (환율) — 클라이언트에서 직접 호출
NEXT_PUBLIC_EXCHANGERATE_API_KEY=your_key_here

# 사이트 URL
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
